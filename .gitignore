node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Env files carry real credentials / connection strings.
.env*
!.env.example

# Local npm cache created because the global cache path is outside the sandbox.
.npm-cache/

# Scratch files (temp commit messages, debug probes).
logs/_*
scripts/_*

# Another tool's working directory. Unrelated to this app and referenced by
# nothing in src/ or server/, so it stays out of this project's history.
.img2threejs-mascot/

# Impeccable workflow scratch: screenshots, harnesses and detector payloads
# captured while running the critique. The written report lives in logs/*.log.
logs/critique/

# OS / editor
Thumbs.db
desktop.ini
.idea/
.vscode/

# Test + tool artifacts
logs/screenshots/
test-results/
playwright-report/
