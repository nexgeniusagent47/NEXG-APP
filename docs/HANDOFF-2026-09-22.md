# HANDOFF — NEXG App

**Date:** 2026-09-25
**Deployed app source:** `b89350d` (`2.2.0`)
**Live:** https://nexgapp.com
**Server:** `root@212.95.32.229` (Ubuntu 24.04.4)
**Spec:** [`SPEC.md`](./SPEC.md) — what the product is. Read that first if you are new.

This supersedes `HANDOFF-2026-09-22.md` earlier in the day and every handoff before it.
`docs/HANDOFF.md` and `docs/HANDOFF-v1.md` are historical and describe a stack that no longer
exists.

## Current production state — 2026-09-25

**This section is the current source of truth and supersedes conflicting historical notes below.**

- **Release:** NEXG App `2.2.0`, commit `b89350d78c1bcacf040f58696d291ceb9aa1035a`, deployed
  at 2026-09-25 06:19 UTC. `/api/version` reports this exact commit. The app image is
  `sha256:aeb8b198c097405ab7f6ed344c507549de866e9a9027b0b01e7b2b01c673cd6f`.
- **Production URLs:** `https://nexgapp.com` and `https://www.nexgapp.com`. Cloudflare now
  uses **Full (strict)**. HTTP on both hostnames redirects to HTTPS. Cloudflare's origin
  certificate is installed at `/etc/ssl/certs/nexgapp-origin-20260925.pem`; its private key is
  root-only at `/etc/ssl/private/nexgapp-origin-20260925.key`. Never commit or copy the key.
- **Nginx:** `/etc/nginx/sites-available/nexgapp.com` is the active site; the tracked template is
  [`../deploy/nginx/nexgapp.com.conf`](../deploy/nginx/nexgapp.com.conf). It redirects port 80,
  serves TLS on 443, and proxies to `127.0.0.1:3101`.
- **Diagnostics:** Zero Trust is deliberately deferred. Until Access and origin validation are
  implemented, Nginx blocks metrics/traces reads (including case, trailing-slash and nested
  variants) with 404, and blocks telemetry reads with 403. Telemetry POST ingestion remains
  available. Verified through both public hostnames after deployment.
- **Health/catalogue:** the app container is healthy and PostgreSQL reports connected:
  21 categories, 128 subcategories, 640 merchants, 6,000 items. The app container alone was
  recreated. PostgreSQL container ID and named data volume `nexg-concierge-pgdata` did not
  change.
- **Release/rollback evidence:** immutable archive at
  `/root/backups/nexg-release-b89350d78c1bcacf040f58696d291ceb9aa1035a.zip`, SHA-256
  `81c67d0be48675e1c7397c00310b1fda08fe0bc2dec7b5b647a4936f7cff8714`; extracted release at
  `/var/www/apps/projects/nexg-concierge/releases/b89350d78c1bcacf040f58696d291ceb9aa1035a`.
  Previous app image is retained as `nexg-concierge-app:rollback-before-b89350d`.
- **Database backup:** `/root/backups/nexg-release-20260925T053027Z.dump` passed a restore
  rehearsal in an isolated temporary Postgres container. Evidence is in the matching
  `*.restore-evidence.txt`. An encrypted off-host copy is still outstanding; no schema or
  data migration was run for this app release.
- **Checks:** GitHub CI and production simulation passed for `b89350d`. After deployment,
  apex/www home, health, version, categories, merchants and search returned 200; HTTP redirected
  to HTTPS; diagnostics returned the blocks listed above.
- **Zero Trust:** no plan was activated and no Access app or policy was created. Read
  [`ZERO-TRUST-ACCESS.md`](./ZERO-TRUST-ACCESS.md) before resuming this work. It explains the
  purpose, boundaries, current temporary protections, and safe future setup.

The TLS setup and deployment runbook is [`DEPLOY-STEP-BY-STEP.md`](./DEPLOY-STEP-BY-STEP.md).

## Historical security addendum — 2026-09-24 (superseded above)

**This addendum supersedes any conflicting live-security or next-priority statement below.**
The user's current priority is to secure and clean the live site before resuming product work.
Follow [`SECURITY-HARDENING-AND-CLEANUP-PLAN.md`](./SECURITY-HARDENING-AND-CLEANUP-PLAN.md)
phase by phase. It is a runbook only; no production changes were made while writing it.

Read-only server snapshot on 2026-09-23:

- Public listeners observed: SSH `:22` and host Nginx HTTP `:80`; no host listener on `:443`.
  UFW was active, default-deny inbound, with 22/80/443 allowed from any address.
- App `127.0.0.1:3101` and PostgreSQL `127.0.0.1:5433` were loopback-bound; PostgreSQL was
  not directly exposed to the internet.
- PostgreSQL was healthy and contained 21 categories, 128 subcategories, 640 merchants, and
  6,000 items. The app's DB authentication failed and it served the smaller JSON fallback
  (120 merchants / 3,300 items). The public health response disclosed the DB error and still
  said `status: ok`.
- `/api/metrics` and `/api/traces` were publicly readable without app authentication in the
  route setup reviewed.
- Current auth source has no operator/admin role and stores users in-process. Do not gate
  operations endpoints with the generic `requireAuth` alone; any signed-up user would pass,
  and the store is not durable. The security runbook recommends a scoped Cloudflare Access
  policy plus origin token validation, or a separately designed durable operator role.
- Cloudflare dashboard read-only review confirmed **Flexible** mode, an active edge Universal
  SSL certificate for the apex and wildcard, and two Origin CA certificates issued for those
  names (expiring 2041). Issuance does not prove either certificate is installed on the server.
  The apex and `www` A records are proxied; email and verification records are DNS-only.
  Cloudflare is on the Free plan. Do not switch to Full (strict) until origin TLS on 443 is
  installed and verified.
- Cloudflare's Security Rules view showed no custom rules, no rate-limit rules, and no managed
  rules created. Its Security Overview simultaneously said “Web app exploits — All running,”
  and Security settings exposed confusing “Disabled: Always active” managed-rule labels.
  Treat managed WAF rule coverage as unverified. HTTP DDoS protection was explicitly marked
  always active; the DDoS page also showed network/SSL protections active. Bot Fight Mode was
  off. No on-demand zone scan had been run.
- A bounded seven-day SSH log review showed successful logins using the key matching the
  existing local public key. It did not establish that every source IP belonged to the owner
  and is not a full forensic conclusion. See the runbook for the IPs to recognize and the
  safe add-test-remove key rotation procedure.

The Cloudflare review was read-only. No DNS/TLS/WAF settings or certificates were changed,
and no scans were run.

Phase 1.1 of the security runbook was completed by the owner on 2026-09-24: a private custom-format
PostgreSQL backup was written to `/root/backups/nexg-2026-09-24-074642.dump` (146K, root-owned,
mode 600), and `pg_restore --list` completed without error. This verifies the archive listing,
not a full restore. An encrypted off-host copy and restore drill remain outstanding. No database
password or application service was changed during this backup step.

Phase 1.2 was then completed by the owner via SSH. The Compose app URL now uses a percent-encoded
password variable; the raw and encoded values were written to the server `.env` with mode 600,
and the stale `DATABASE_URL` entry was removed. Protected config copies were reported saved as
`/root/backups/nexg-compose-2026-09-24-092002.yml` and
`/root/backups/nexg-env-2026-09-24-092002.env`. `docker compose config -q` succeeded. Only the
app container was recreated; both app and PostgreSQL reported healthy, and PostgreSQL stayed
up with its host port bound to `127.0.0.1:5433`. The filtered health result changed from JSON
fallback/authentication failure to `source=postgres`, `postgresConnected=True`, 640 merchants,
and 6,000 items. A public storefront check, off-host backup, and restore drill are still pending.

The repository working tree now mirrors the production Compose URL fix and updates the local
environment example, PostgreSQL setup script, and deployment instructions. These source and
documentation edits are uncommitted; the live edit was performed by the owner over SSH. The
server's quiet Compose validation passed. This workstation has no Docker CLI, so a second local
Compose validation was unavailable; the modified PowerShell setup script passed a syntax parse.

### 2026-09-24 local catalogue fail-closed patch — not deployed

At the owner's request, the local source removes `src/data/seededCatalog.json` and
`src/data/catalogData.ts`, removes server-side JSON catalogue fallback behavior, and changes
health/readiness and catalogue endpoints to return HTTP 503 when PostgreSQL cannot answer.
Discovery, category, and merchant/item search now load from the API; the production image
does not copy the JSON seed. The Docker healthcheck now depends on a real database-backed
`/api/health` query. The PostgreSQL data volume and the verified backup remain untouched.

The local change has not been deployed. The live server therefore remains on its previous
image until the owner approves a release. `node node_modules/typescript/bin/tsc --noEmit` and
`git diff --check` passed. Full application tests/build and a database-outage simulation were
not run during preparation.
After an approved deployment, confirm `/api/health` returns 200 with 640 merchants and 6,000
items, public discovery/search/category requests use PostgreSQL, and a controlled staging outage
returns 503 without a fallback catalogue. Do not simulate an outage on production.

### 2026-09-24 onboarding and ecosystem visual pass — local, not released

Onboarding headings now use the homepage Quicksand face at its real 700 weight, shared select
chevrons align across the merchant, courier, and host forms, and the app's standard rounded
utilities use reversible elliptical x/y radii. The supplied wordmark's N is slightly heavier in
both theme assets. All wordmark gold artwork now matches the site tokens (`#B88728` in light
mode, `#E5B65F` in dark mode), and the compact bell-G mark uses that theme token too. Properties
ecosystem icon glyphs and hover accents now use one theme-aware brand gold.

Noun Project downloads remain pending. Its public free CC BY flow presented account creation and
Terms of Use acceptance; neither was completed. No user account was created and no paid license
was purchased. Resume after the owner signs in and provides four CC BY downloads, preserving the
creator credit in an app-visible attribution location. No test/build, release, or deployment was
performed for this pass.

### 2026-09-25 server-backed onboarding — local, not released

Merchant, courier, and host drafts now save through Express to PostgreSQL; each flow resumes
from a role-specific HttpOnly cookie, and submissions pass server-side validation before the
success screen appears. Payloads are encrypted with AES-256-GCM using a key derived from the
stable `AUTH_SECRET`; keep that secret unchanged while applications need to remain readable.
Drafts expire after 30 days and are deleted on the next draft read, save, or submission request.
The additive SQL is in
[`../src/db/migrations/20260925_onboarding_applications.sql`](../src/db/migrations/20260925_onboarding_applications.sql)
and the local project's database on `127.0.0.1:5433` has it applied. Production has not been
migrated or deployed.

The current upload controls send filenames only; document bytes are still not uploaded. There is
also no staff/admin read view for submitted applications yet. Submission records have no public
read endpoint. `npx tsc --noEmit` and `npm run build` passed; the API was restarted and booted
against local PostgreSQL. Formal tests were not run. Vite remains running on `:3000`.

The host form still contains a pre-existing Impeccable variant. Its live status was
`agentPolling: true`, `connectedClients: 3` with a pending render; this pass left the variant
markup unchanged and did not accept or discard it.

Other statements below about public exposure, origin TLS, operator endpoints, a full compromise
sweep, or mobile priority remain historical until independently reverified. The database
connection recovery does not establish those controls.

---

## 0. Read this much, at minimum

The user's own words, unedited, because they are the brief:

> **"THE MOBILE VIEWS ARE SHIT"**
> **"i need our mobile to look like for wolt"**
> **"DONT DIAGNISE ANYTHING, WRITE PROJECT SPEC AND A PROPER HANDOFF THATS ALL I ASK"**

**The single most important fact in this document: mobile is not done, the user says so in
those words, and they are right.** Do not start by claiming otherwise or by re-measuring what
is already measured below. Start by reading section 3 and fixing it.

**The second: this project has repeatedly produced green gates over broken software.** Section
5 lists six specific ways that has happened here. Read it before trusting any check.

---

## 1. Where everything is

| | |
| --- | --- |
| Local repo | `C:\Users\limta\Downloads\nexg-concierge` |
| SSH key | `C:\Users\limta\.ssh\nexg_deploy` (private), `.pub` beside it |
| Server | `root@212.95.32.229`, key-only |
| App directory | `/var/www/apps/projects/nexg-concierge` |
| Old backups | `C:\Users\limta\Downloads\nexg-old-backups\` — **local only, 177 MB** |

### The running system

```
app        127.0.0.1:3101   Express: API + built SPA
postgres   127.0.0.1:5433   Compose service port 5432
nginx      0.0.0.0:80/443   redirect + TLS reverse proxy; the only public entry point
```

Both containers publish on **loopback only**.

### Local services — the user has closed the dev server

```
Vite    :3000   npm run dev
API     :3001   npm run server
Postgres:5433   docker
```

**Do not touch ports 5432 or 8080.** They belong to an unrelated NEXG POS Go platform on the
same machine. This has caused confusion more than once.

---

## 2. What is deployed and working

| | |
| --- | --- |
| Site | https://nexgapp.com — HTTP 200 |
| Health | `source=postgres` · 21 categories · 128 subcategories · **640 merchants · 6,000 items** |
| Rebrand | **NEXG App** everywhere; `NEXG Concierge` appears 0 times in the bundle |
| Logo | the supplied wordmark, inline SVG, on 9 surfaces |
| Favicon | the G with the bell, light and dark variants |
| Rate limiting | verified `401×10 → 429` |
| Security headers | live, verified through nginx |
| Client identity | IP + device on every span and log line |

### Security controls in place

Key-only SSH · fail2ban (5 IPs banned from **17,289 attempts in 24 h**) · unattended upgrades ·
MySQL port closed · **`APP_DEBUG=false`** · passwords rotated and purged · CSP · HSTS · rate
limiting · scrypt.

A full compromise sweep found **no breach**: no suspicious processes, no orphan containers, only
our own `authorized_keys`, no outbound beacons.

### The old application

**Gone.** It was Vue + Laravel + **MySQL** at `/var/www/apps/projects/nexg/`. Containers, images,
compose file, Dockerfile, nginx vhost and 2.5 GB of build cache all removed.

**The only backup is on the user's local machine.** Verified restorable: the SQL dump
decompresses to 4.09 MB with 198 `CREATE TABLE` and 73 `INSERT` statements including `orders`,
`order_items` and `personal_access_tokens`. **If that folder is lost, the old site's data is
gone permanently.**

Its data was **not** migrated. The 63 orders and 283 access tokens do not exist in our Postgres.
The user said the old site was rubbish and to start over, so this was intentional — but say so
out loud rather than letting someone discover it later.

---

## 3. THE PRIORITY: mobile

The user has raised this **four times** and is frustrated. Treat it as the only task until it is
resolved.

### The reference

The user shared **Wolt** screenshots. The specific qualities to match:

| Wolt | Ours |
| --- | --- |
| Rounded-**square category tiles** with full-bleed photography, label underneath | **pill chips with emoji** |
| Merchant cards: image, title, subtitle, **dashed divider**, meta row | similar, spacing inconsistent |
| Docked search with a **location chip** | search only |
| Product list with a **sticky category tab strip** | **missing** |
| Generous section rhythm, ~48px | tighter |
| Light, airy, high contrast | dark; **secondary text is low contrast** |

### What is actually done

- Card titles no longer clip. They were `h-10` (40px) holding two lines needing 77px.
- Nav and hamburger hand over at `sm`; no width shows both. **Hamburger is gone from desktop.**
- The mobile drawer closes on navigation and locks/unlocks `body.overflow`.
- Category and subcategory rails show emoji, verified at 360px and 1440px.
- A docked search bar with react-spring.

### What is NOT done — this is the list

1. **Image tiles, not pill chips.** This is the visible gap and the thing the user keeps asking
   for. Wolt's rail is squares with photography. Ours is pills with emoji. **The emoji helped;
   it is not the same object.**
2. **Merchant card rhythm** to Wolt's spacing and dashed divider.
3. **Product-row category tab strip** on the merchant page, sticky, scrolling to sections.
4. **Secondary-text contrast on dark.** The teal-on-near-black in the screenshots is genuinely
   low contrast. **Measure it; do not eyeball it.**
5. **Responsive proof across all 18 devices.** See below.

### Before relying on any image field, check it

The catalogue **looks** like it carries what the rails need. Measured, it does not:

```
every category    icon_name: "Sparkles"   ← all 21, the same generic icon
every subcategory image_url: ""           ← empty string
```

That is why the rails rendered as bare text for so long. **Verify data before designing
against it.** `src/data/railEmoji.ts` holds the emoji map built to work around it.

### The device matrix has NOT been re-run

`logs/critique/_device-matrix.mjs` tests **18 real phone sizes × 4 pages = 72 combinations** —
iPhone SE through 16 Pro Max, Galaxy S8 through S24 Ultra, Fold, and Tecno/Infinix/Redmi at
360px. The widths that break layouts are **320, 344 and 360**.

It found **37 of 72 broken.** Two causes are fixed. **The rest are unverified.**

```bash
npm run dev          # needs Vite on :3000
node logs/critique/_device-matrix.mjs
```

**Known non-bug:** marquees and carousels extend past the viewport by design. A 1880px rail
inside a 360px scroller is correct. Do not "fix" it.

---

## 4. Other open work

### 4.1 HTTPS — configured on 2026-09-25

Cloudflare uses **Full (strict)** and the server has a matching Cloudflare Origin CA
certificate on port 443. Public HTTP redirects to HTTPS. Both apex and `www` were verified
after cutover. The older Flexible-mode diagnosis and Origin CA access error above describe the
2026-09-23/24 snapshot only; they are not current.

See the current production state at the top of this handoff and the tracked Nginx template at
[`../deploy/nginx/nexgapp.com.conf`](../deploy/nginx/nexgapp.com.conf). Do not disable certificate
validation or switch back to Flexible. Zero Trust is separate and remains deferred.

### 4.2 Observability OS — needs a decision

The user wants an open-source OS-level observability stack. **Recommendation: OpenObserve** —
single Rust binary, ~1–2 GB, all four signals. SigNoz needs 4 GB minimum and the server has
16 GB total with the app running. Grafana + LGTM is 4–6 GB.

Existing in-process instrumentation is already OTel-shaped, so an exporter is configuration.

### 4.3 Smaller items

- `LOG_LEVEL=debug` in production.
- `/api/version` now reports the deployed commit. Keep passing `GIT_SHA` and `BUILT_AT` when
  building from a release archive; archives do not contain `.git` history.
- The catalogue's generated merchant data has **no real menu sections**. Highest-value data fix.
- Verify the pending security updates actually applied via `unattended-upgrades`.

---

## 5. Six traps that have already cost real time

Not style preferences. Each caused a wrong answer, a wasted hour, or a silent failure.

### 5.1 A green gate is not working software

`tsc` reported clean while **add-to-cart was dead on five pages**, because `@types/react` was
missing and errors vanished. Installing it exposed **19 live defects** in one run.

**Never remove `@types/react` or `@types/react-dom`.**

### 5.2 A clean scan is not evidence without a control

The detector timed out on every URL scan, **exited 0, and printed `[]`** — a failed scan was
byte-identical to a clean one, and 22 such results were reported as passes. **Always run a
known-bad input in the same run.**

### 5.3 `skip-worktree` hides changes from git AND `git archive`

`index.html` carried the `S` flag. A rebrand edit lived only in the working copy, was never
committed, and **every deploy shipped stale HTML while all gates stayed green**.

If a deploy serves the wrong content but the source looks right: `git ls-files -v <file>`.
**Do not re-add the flag.**

### 5.4 The build happens INSIDE the Docker image

There is **no `dist/` on the server.** An earlier deploy script checked the host filesystem and
**reported zero occurrences because the path did not exist** — a failed check identical to a
clean one. Verify against the running container:

```bash
docker exec nexg-concierge-app-1 sh -c 'ls /app/dist/assets'
```

### 5.5 PowerShell 5.1 corrupts, mangles and misleads

- **`>` re-encodes binary as UTF-16.** Streaming a tar produced `FF FE 1F 00` instead of
  `1F 8B 08` and inflated 63 MB to 120 MB. **Use `cmd /c "... > file"`.**
- **`Out-File`/`Set-Content` are not UTF-8.** Use
  `[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($false)))`.
- **It eats quotes, backticks and em-dashes.** A commit message containing `→` dies with
  `error: unknown switch`. **Write the message to a file and use `git commit -F`.**
- **Its console renders valid UTF-8 as mojibake**, which twice looked like file corruption.
  **Verify at byte level.**
- **Inline `node -e` with nested quotes fails.** Write a `.mjs` file instead. This wasted four
  attempts in one session.

### 5.6 Node 24 runs `server/*.ts` in STRIP-ONLY mode

Constructor parameter properties, `enum`, `namespace` and decorators throw
`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` **at import time and kill the process**, while
`tsc --noEmit` passes happily. **After any `server/` change, actually boot it.**

---

## 6. Instrument traps that produced wrong answers

- **Canvas returns opaque black for `oklch()`** — it cannot parse it. Two colour probes gave
  confidently wrong results. **Read raw computed values.**
- **`window.scrollTo` reports `scrollY: 0` when an input holds focus.** Use `mouse.wheel`.
- **`height / line-height` is not a line count.** Use `Range.getClientRects()`.
- **`scp -r` is slow for many small files** — 10,510 files took over 10 minutes. Tar over SSH.
- **A temp-dir script cannot `require('playwright')`.** Keep scripts inside the project.
- **Don't compute SVG bounds by parsing path data.** Relative commands make it meaningless —
  producing negative coordinates for a shape in the positive quadrant. Use `getBBox()` or an
  alpha scan, or render each element and look.

---

## 7. Process rules

1. **Skill discovery first.** Run `find-skills` before choosing an approach. There are **312
   skills** in `~/.agents/skills`; only **91** are in `.skill-lock.json`, so `npx skills update`
   silently ignores the other 221.
2. **Measure, do not eyeball.** Sizes, contrast, positions, counts all get a number. The clipped
   card title was 40px against 77px of content and invisible until measured.
3. **Report what is not verified.** "Not run" and "failed" are different answers; both beat an
   implied pass.
4. **The user decides deployment.** They have said: *"WE APPROVE SLOWLY SLOWLY, YOU NEED MY
   APPROVAL FOR EVERYTHING."* Back up before anything destructive — that instinct saved the old
   site's 63 orders.

### Commit messages

Keep them substantive and record **why**, not what. The user has repeatedly found real value in
commit messages that explain a trap. Write the message to a file first (see 5.5).

---

## 8. Do not break these

- **Do not remove `@types/react` / `@types/react-dom`.**
- **Do not re-add `skip-worktree` to `index.html`.**
- **Do not commit credentials.** `logs/deploy/*` were redacted; verify with `git grep` first.
- **Do not run `docker compose down -v`.** It deletes the catalogue volume.
- **Do not touch ports 5432 or 8080.**
- **Do not raise heading `font-weight` above 700.** Quicksand's axis stops there; higher renders
  **synthetic** bold, which reads lighter, not heavier.
- **One animation library per component subtree.** Motion and react-spring both write
  `transform`; two writers on one node means the last one wins.
- **Never put `127.0.0.1` in the container's `DATABASE_URL`.** Inside the app container that is
  the container itself, so the app silently serves the bundled JSON catalogue while reporting
  healthy. Use the compose service name `postgres`.

---

## 9. Deployment

Full runbook in [`DEPLOY-STEP-BY-STEP.md`](./DEPLOY-STEP-BY-STEP.md).

The current release was deployed from a fresh, commit-pinned archive under `releases/<sha>`.
Do not overwrite the stable server directory or copy `.env` into the repository. Follow the
runbook's release-directory, image-tag rollback, app-only recreation, and smoke-check steps.

**`PORT=3101` in `.env`** drives both the host binding and the container listen port. They must
agree — setting one and not the other cost a verification round.

`logs/deploy/` holds every script used, as the audit trail, **with credentials redacted**.

---

## 10. First three actions

1. **Start the services** — the user closed them. `npm run dev` and `npm run server`. Everything
   below needs them, and their absence has twice been misread as a broken test suite.
2. **Re-run the device matrix.** `node logs/critique/_device-matrix.mjs`. 35 of 72 combinations
   are unverified and the user is looking at phones. Reporting a clean run would be genuinely
   new information — it has never been clean.
3. **Build the image-tile rail.** That is the single change that moves the mobile experience
   toward the reference, and it is what the user has asked for four times.

**Then:** HTTPS (route B needs no Cloudflare access), and the Wolt card rhythm.
